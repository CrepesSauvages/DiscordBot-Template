<!DOCTYPE html>
<html>
<head>
    <title><%= guild.name %> - Dashboard</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="dashboard-container">
        <header>
            <div class="guild-header">
                <img src="<%= guild.icon ? `https://cdn.discordapp.com/icons/${guild.id}/${guild.icon}.png` : '/img/default_guild.png' %>" 
                     alt="<%= guild.name %>">
                <h1><%= guild.name %></h1>
            </div>
            <div class="user-info">
                <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar">
                <span><%= user.username %></span>
                <a href="/auth/logout" class="logout-btn">Déconnexion</a>
            </div>
        </header>

        <nav class="guild-nav">
            <a href="/guild/<%= guild.id %>" class="active">Vue d'ensemble</a>
            <a href="/guild/<%= guild.id %>/commands">Commandes</a>
            <a href="/guild/<%= guild.id %>/settings">Paramètres</a>
        </nav>

        <main>
            <div class="guild-stats">
                <div class="stat-card">
                    <h3>Membres</h3>
                    <p><%= guild.memberCount %></p>
                </div>
                <div class="stat-card">
                    <h3>Canaux</h3>
                    <p><%= guild.channels.cache.size %></p>
                </div>
                <div class="stat-card">
                    <h3>Rôles</h3>
                    <p><%= guild.roles.cache.size - 1 %></p>
                </div>
            </div>

            <div class="quick-actions">
                <h2>Actions rapides</h2>
                <div class="actions-grid">
                    <a href="/guild/<%= guild.id %>/settings" class="action-card">
                        <i class="fas fa-cog"></i>
                        <span>Paramètres</span>
                    </a>
                    <a href="/guild/<%= guild.id %>/commands" class="action-card">
                        <i class="fas fa-terminal"></i>
                        <span>Commandes</span>
                    </a>
                </div>
            </div>
        </main>
    </div>

    <script src="https://kit.fontawesome.com/your-kit-code.js"></script>
    <script>
        const translations = <%- JSON.stringify(translations) %>;
    </script>
    <script src="/js/guild.js"></script>
</body>
</html> 